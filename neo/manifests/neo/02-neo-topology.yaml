---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: neo-topology
  namespace: neo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: neo-topology
  template:
    metadata:
      labels:
        app: neo-topology
        version: "v1"
        k8s-app: neo-topology
    spec:
      imagePullSecrets:
        - name: gcr-access-token
      containers:
        - name: neo-topology
          image: gcr.io/traefiklabs/neo-topology:latest
          imagePullPolicy: Always
          livenessProbe:
            httpGet:
              port: 80
              path: /live
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 2
          readinessProbe:
            httpGet:
              port: 80
              path: /ready
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 2
          env:
            - name: NEO_JWT_CERT
              valueFrom:
                secretKeyRef:
                  key: jwt-cert
                  name: neo-secret
            - name: TOKEN_SERVICE_URL
              value: http://neo-token.neo.svc.cluster.local/internal
            - name: NEO_JWT_ISS
              value: "https://traefiklabs-neo-dev.eu.auth0.com/"
            - name: NEO_JWT_SUB
              valueFrom:
                secretKeyRef:
                  key: jwt-sub
                  name: neo-secret
            - name: GITHUB_TOKEN
              valueFrom:
                secretKeyRef:
                  name: github
                  key: token
            - name: GITHUB_ORG
              valueFrom:
                secretKeyRef:
                  name: github
                  key: org
            - name: TRACING_ENDPOINT
              value: "http://jaeger.jaeger.svc.cluster.local:14268"
            - name: TRACING_PROBABILITY
              value: "1"
            - name: LOG_LEVEL
              value: debug
          args:
            - serve
            - --addr=:80
            - --git-proxy-host=platform.docker.localhost

---
apiVersion: v1
kind: Service
metadata:
  name: neo-topology
  namespace: neo
  labels:
    app: neo-topology
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      name: neo-topology
  selector:
    app: neo-topology

---
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: topology-external-tls
  namespace: neo
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`platform.docker.localhost`) && PathPrefix(`/topology/external`)
      services:
        - name: neo-topology
          port: 80
      middlewares:
        - name: stripprefix
  tls:
    certResolver: default

---
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: topology-external
  namespace: neo
spec:
  entryPoints:
    - web
  routes:
    - kind: Rule
      match: Host(`platform.docker.localhost`) && PathPrefix(`/topology/external`)
      services:
        - name: neo-topology
          port: 80
      middlewares:
        - name: stripprefix
