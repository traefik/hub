apiVersion: hub.traefik.io/v1alpha1
kind: APIGateway
metadata:
  name: my-gateway
  labels:
    area: stores
spec:
  customDomains:
    - my-gateway.tls.docker.localhost

  apiAccesses:
    - my-stores-access
